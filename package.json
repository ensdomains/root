{
  "name": "@ensdomains/root",
  "version": "0.0.1",
  "requires": true,
  "lockfileVersion": 1,
  "devDependencies": {
    "@ensdomains/buffer": "^0.0.6",
    "@ensdomains/dnsregistrar": "^0.2.2",
    "@ensdomains/dnssec-oracle": "^0.0.8",
    "@ensdomains/ens": "^0.3.0",
    "bluebird": "^3.5.2",
    "eth-ens-namehash": "^2.0.8",
    "eth-gas-reporter": "^0.1.2",
    "solium": "^1.2.3",
    "truffle": "^4.0.0"
  },
  "scripts": {
    "pretest": "truffle compile",
    "test": "truffle test",
    "watch": "npm-watch",
    "lint": "solium --dir ./contracts",
    "prepublishOnly": "truffle compile && truffle networks --clean"
  }
}
